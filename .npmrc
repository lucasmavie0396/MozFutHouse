# Evita compilar binários nativos do better-sqlite3 (node-gyp precisa de
# Python + Visual Studio Build Tools). O pacote inclui binários NAPI
# pré-compilados que funcionam sem scripts.
ignore-scripts=true